{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/leighmcculloch/silo/main/silo.schema.json",
  "title": "Silo Configuration",
  "description": "Configuration file for silo - run AI coding tools in isolated containers",
  "type": "object",
  "properties": {
    "$schema": {
      "type": "string",
      "description": "JSON Schema reference for editor autocompletion and validation"
    },
    "backend": {
      "type": "string",
      "enum": ["docker", "container"],
      "description": "Backend to use for running containers. 'docker' uses Docker, 'container' uses Apple's lightweight VMs. Default: 'container' if installed, else 'docker'",
      "examples": ["docker", "container"]
    },
    "tool": {
      "type": "string",
      "description": "Default tool to run. If not set, an interactive prompt is shown.",
      "examples": ["claude", "opencode", "copilot"]
    },
    "mounts_ro": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Read-only directories or files to mount into the container. Paths starting with ~ are expanded to home directory.",
      "examples": [["~/.gitconfig", "~/.ssh/known_hosts"]]
    },
    "mounts_rw": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Read-write directories or files to mount into the container. Paths starting with ~ are expanded to home directory.",
      "examples": [["~/.cache/myapp"]]
    },
    "env": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Environment variables. Names without '=' pass through from host, names with '=' set explicitly (e.g., 'FOO=bar').",
      "examples": [["MY_API_KEY", "DEBUG=1"]]
    },
    "pre_run_hooks": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Shell commands to run inside the container before the tool starts. Useful for dynamic setup that depends on the mounted working directory.",
      "examples": [["cd /workspace && npm install"]]
    },
    "post_build_hooks": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Shell commands to run inside the container after building the image. These are baked into the image and cached.",
      "examples": [["apt-get update && apt-get install -y ripgrep", "npm install -g typescript"]]
    },
    "tools": {
      "type": "object",
      "description": "Tool definitions and configuration. Each key is a tool name. Built-in tools (claude, opencode, copilot) have defaults; you can override any field or define entirely new tools.",
      "additionalProperties": {
        "$ref": "#/$defs/toolConfig"
      },
      "examples": [{
        "claude": {
          "description": "Claude Code - Anthropic's CLI for Claude",
          "dockerfile": "FROM base AS claude\nARG HOME\nARG TOOL_VERSION\nRUN set -e && ...\n# SILO_POST_BUILD_HOOKS_CLAUDE",
          "command": ["$HOME/.local/bin/claude", "--mcp-config=$HOME/.claude/mcp.json", "--dangerously-skip-permissions"],
          "latest_version_url": "https://storage.googleapis.com/claude-code-dist-.../claude-code-releases/latest",
          "mounts_rw": ["~/.claude.json", "~/.claude"]
        }
      }]
    },
    "repos": {
      "type": "object",
      "description": "Repository-specific configuration. Each key is a substring to match against git remote URLs (prefix matching). When the current directory is a git repository and any remote URL contains the key as a substring, that configuration is applied. Multiple patterns can match the same repo; they are applied in order of specificity (shortest pattern first), so more specific patterns extend or override less specific ones.",
      "additionalProperties": {
        "$ref": "#/$defs/repoConfig"
      },
      "examples": [{
        "github.com/myorg": {
          "tool": "opencode",
          "env": ["ORG_API_KEY"],
          "post_build_hooks": ["npm install -g @myorg/cli"]
        },
        "github.com/myorg/specific-repo": {
          "tool": "claude",
          "env": ["REPO_SPECIFIC_VAR"],
          "pre_run_hooks": ["echo 'Setting up specific-repo'"]
        }
      }]
    }
  },
  "$defs": {
    "toolConfig": {
      "type": "object",
      "description": "Full definition and configuration for a tool. Definition fields (description, dockerfile, command, etc.) define what the tool is. Configuration fields (mounts, env, hooks) define how it runs. Built-in tools already have defaults for all definition fields.",
      "properties": {
        "description": {
          "type": "string",
          "description": "Human-readable description of the tool (e.g. 'Claude Code - Anthropic's CLI for Claude')."
        },
        "dockerfile": {
          "type": "string",
          "description": "Dockerfile stage text. Must start with 'FROM base AS <toolname>'. Include '# SILO_POST_BUILD_HOOKS_<TOOLNAME>' as a marker for hook injection."
        },
        "command": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Container entrypoint and arguments. Use $HOME for the user's home directory (expanded at runtime)."
        },
        "latest_version_url": {
          "type": "string",
          "format": "uri",
          "description": "URL that returns the latest version as plain text. Used for auto-update detection and cache-busting."
        },
        "latest_version_github_release": {
          "type": "string",
          "description": "GitHub 'owner/repo' whose latest release tag_name is used as the version. Alternative to latest_version_url.",
          "pattern": "^[^/]+/[^/]+$"
        },
        "mounts_ro": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Read-only directories or files to mount for this tool only."
        },
        "mounts_rw": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Read-write directories or files to mount for this tool only."
        },
        "env": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Environment variables for this tool only. Same format as global env."
        },
        "pre_run_hooks": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Shell commands to run inside the container before this tool starts."
        },
        "post_build_hooks": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Shell commands to run in the Dockerfile for this tool's build stage."
        }
      },
      "additionalProperties": false
    },
    "repoConfig": {
      "type": "object",
      "description": "Configuration specific to a git repository. Applied when any git remote URL contains the key as a substring. When multiple patterns match, configs are merged in order of specificity (shortest pattern first).",
      "properties": {
        "tool": {
          "type": "string",
          "description": "Tool to use for this repository (e.g., 'claude', 'opencode', 'copilot')."
        },
        "mounts_ro": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Read-only directories or files to mount for this repository."
        },
        "mounts_rw": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Read-write directories or files to mount for this repository."
        },
        "env": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Environment variables for this repository. Same format as global env."
        },
        "pre_run_hooks": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Shell commands to run inside the container before the tool starts."
        },
        "post_build_hooks": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Shell commands to run in the Dockerfile."
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
