package ssh

import "github.com/leighmcculloch/silo/config"

// SSHBackendConfig is an alias for config.SSHBackendConfig so that
// code within this package can reference the type without a qualifier.
type SSHBackendConfig = config.SSHBackendConfig
